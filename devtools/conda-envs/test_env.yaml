name: test
channels:

  - defaults
  - conda-forge
  - bioconda

dependencies:
    # Base depends
  - python
  - pip
  - autodock-vina
  - openbabel

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - qcengine
    - pydantic
    # mmelemental
    # mmic
    # mmic_translator
