name: test
channels:

  - defaults
  - conda-forge
  - bioconda

dependencies:
    # Base depends
  - python
  - pip
  - autodock-vina
  - openbabel

    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - pydantic
    - qcengine
    - qcelemental
    # mmelemental
    # mmic
    # mmic_docking
    # mmic_translator
